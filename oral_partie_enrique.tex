% ==========================================================================
% ORAL — PARTIE ENRIQUE (slides 11 a 22)
% Script precis : tout ce qu'il faut dire, mot pour mot.
% Compilable avec pdflatex.
% ==========================================================================
\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{lmodern}
\usepackage[a4paper,margin=2.2cm]{geometry}

\usepackage{amsmath,amssymb,bm}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{tcolorbox}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{array}
\usepackage{fancyhdr}
\usepackage{listings}

\hypersetup{colorlinks=true, linkcolor=blue!60!black, urlcolor=blue!60!black}

% Style des blocs de code Python
\definecolor{codebg}{rgb}{0.97,0.97,0.97}
\definecolor{codekw}{rgb}{0.13,0.29,0.53}
\definecolor{codestr}{rgb}{0.50,0.20,0.00}
\definecolor{codecom}{rgb}{0.25,0.50,0.25}
\lstdefinestyle{py}{
    language=Python,
    backgroundcolor=\color{codebg},
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{codekw}\bfseries,
    stringstyle=\color{codestr},
    commentstyle=\color{codecom}\itshape,
    showstringspaces=false,
    breaklines=true,
    frame=single,
    rulecolor=\color{gray!40},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=6pt,
    captionpos=b,
    tabsize=4,
    columns=fullflexible
}
\lstset{style=py}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 1pt}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Oral — Partie Enrique}
\fancyhead[R]{Battle Météorage}
\fancyfoot[C]{\thepage}

\newtcolorbox{dire}{colback=blue!4, colframe=blue!50!black, title=\textbf{À dire (mot pour mot)}}
\newtcolorbox{compr}{colback=green!4, colframe=green!50!black, title=\textbf{Ce qu'il faut comprendre}}
\newtcolorbox{piege}{colback=orange!4, colframe=orange!60!black, title=\textbf{Piège / question possible du jury}}
\newtcolorbox{transit}{colback=gray!5, colframe=gray!50!black, title=\textbf{Transition}}

\title{\textbf{Oral — Partie Enrique} \\[0.3em]
       \large Script précis pour les slides 11 à 22 \\
       \small Battle Météorage --- Weibull AFT + protocole Data Battle}
\author{}
\date{}

% ==========================================================================
\begin{document}
\maketitle

\begin{tcolorbox}[colback=yellow!8, colframe=yellow!50!black, title=\textbf{Mode d'emploi}]
\textbf{Tu présentes les slides 11 à 22.} Pour chaque slide :
\begin{itemize}[leftmargin=*, topsep=2pt]
    \item \textbf{À dire} = phrase à dire telle quelle (en bleu)
    \item \textbf{Ce qu'il faut comprendre} = ce que le concept veut dire en français simple (en vert)
    \item \textbf{Piège} = question pénible que le jury peut poser, et la réponse
    \item \textbf{Transition} = phrase pour enchaîner vers le slide suivant (en gris)
\end{itemize}

Lis les blocs bleus en gardant les yeux sur le public. Les autres sont pour toi en cas de question.

\textbf{Important} : si tu galères sur un slide, dis juste les blocs bleus, ils suffisent.
Les autres sont du bonus pour répondre aux questions.
\end{tcolorbox}

\tableofcontents
\newpage

% ==========================================================================
\section{Slide 11 — Le piège : on annonce 1 \%, on a 6 \% en réalité}

\begin{dire}
\og On a notre Weibull AFT qui prédit $T_q$. C'est bien, mais avant de le déployer,
il faut vérifier qu'il est \textbf{bien calibré}.

Calibré veut dire : si j'annonce 1 \% de risque, j'ai \emph{vraiment} 1 \% de risque, pas 6 \%.

Et sur nos données de test, le modèle n'est pas calibré. Quand on annonce 1 \% de risque,
en réalité 6 \% des observations dépassent le quantile prédit. Pour 5 \% on est à 14 \%,
pour 10 \% on est à 21 \%. C'est entre 2 et 6 fois plus que ce qu'on annonce.

Pourquoi ? Parce que la queue de la loi de Weibull décroît très vite, en
$\exp(-(t/\lambda)^k)$. Mais les vrais longs silences entre éclairs sont \emph{encore plus
longs} que ce que Weibull prédit. Le modèle est trop optimiste sur les extrêmes.
\fg{}
\end{dire}

\begin{compr}
\textbf{Calibration} = est-ce que ce qu'on annonce correspond à ce qui se passe vraiment ?

Exemple Météo France : quand le bulletin annonce \og 30 \% de probabilité de pluie demain\fg{},
on attend qu'il pleuve effectivement \emph{environ 30 \%} des jours où ce bulletin sort.

Pour notre modèle : quand on annonce \og après $T_q$ minutes, il y a 1 \% de chance qu'un éclair
arrive\fg{}, on aimerait que ce soit vrai. Mais sur le test, ce n'est pas 1 \%, c'est 6 \%.
Donc le modèle est \textbf{trop optimiste} : il sous-estime le risque réel.

C'est comme un thermomètre qui affiche toujours 2 degrés de moins que la vraie température :
il faut le \emph{corriger} avant de l'utiliser.
\end{compr}

\begin{piege}
\textbf{Q : Pourquoi le Weibull est-il sous-calibré ?}

R : Parce que la queue Weibull décroît comme une exponentielle. Or les vrais longs silences
intra-orage suivent plutôt une décroissance plus lente (heavy tail). Le Weibull \og sous-estime
les valeurs extrêmes\fg{} — donc ses quantiles élevés ($T_{99}$) sont trop courts par rapport
à la réalité.
\end{piege}

\begin{transit}
\og C'est pour ça qu'on a ajouté une calibration empirique robuste. Slide suivant. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 12 — Solution : winsorization + monotonicité}

\begin{dire}
\og La solution : une \textbf{calibration empirique en deux étapes}.

\textbf{Étape 1 — winsorization.} On regarde les résidus, c'est-à-dire le rapport entre
le temps réel observé et le temps $T_q$ prédit par Weibull. Si on est bien calibré, ce rapport
vaut environ 1. Si on est sous-calibré, il est plus grand.

On calcule le P95 de ces résidus, et on remplace toute valeur au-dessus du P95 par la valeur du P95.
Ça neutralise les outliers extrêmes sans les supprimer. Ensuite on prend le quantile $q$
des résidus winsorisés : c'est notre coefficient $c_q$.

\textbf{Étape 2 — monotonicité.} Mathématiquement, $T_{99} > T_{95} > T_{90}$.
Mais après winsorization, on a parfois $c_{99} < c_{95}$, ce qui rendrait $T_{99} < T_{95}$,
absurde. On impose donc :
$c_q^{\text{final}} = \max(c_q^{\text{winsorisé}},\ c_{q-1}^{\text{final}})$.

Au final, le quantile calibré est $T_q^{\text{cal}}(X) = c_q \cdot T_q^{\text{Weibull}}(X)$.
\fg{}
\end{dire}

\begin{compr}
\textbf{Winsorization} : on coupe les valeurs extrêmes au lieu de les supprimer.

Exemple : tu as les notes 8, 10, 12, 14, 20. La 20 est un outlier. Au lieu de la supprimer,
tu la remplaces par 14 (le P80 par exemple). Tu gardes l'information \og c'était une grosse note \fg{}
mais sans qu'elle pèse trop dans la moyenne.

\textbf{Monotonicité} : la sécurité 1 \% doit donner un temps d'attente \emph{plus long} que la sécurité 5 \%.
Sinon, c'est incohérent.

\textbf{Le résultat} : un facteur multiplicatif $c_q$ qu'on applique au Weibull brut.
Par exemple $c_{95} = 2{,}02$ veut dire \og pour vraiment avoir 5 \% de risque, il faut multiplier
le $T_{95}$ Weibull par 2 \fg{}.
\end{compr}

\begin{piege}
\textbf{Q : Pourquoi ne pas avoir choisi une autre loi (Gamma, Log-normale) au lieu de corriger ?}

R : On a essayé. Weibull est le standard en survie pour les inter-arrivées (référence dans
les manuels de fiabilité industrielle). En plus, lifelines le supporte nativement avec censure.
Le post-hoc de calibration empirique est plus rapide à mettre en œuvre et donne des résultats
robustes — on aurait pu utiliser une non-paramétrique, mais on perd la régression sur les
covariables qui est le point clé.
\end{piege}

\begin{transit}
\og Maintenant qu'on a un modèle calibré, on vérifie nos résultats avec une approche
\emph{complètement indépendante}. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 13 — Vérification empirique non-paramétrique}

\begin{dire}
\og On veut une vérification \textbf{indépendante du modèle}, sans aucune hypothèse de distribution.

L'idée est simple : pour chaque alerte historique, on calcule $G(A)$, le plus grand silence
intra-alerte — c'est-à-dire le plus grand intervalle entre deux éclairs consécutifs.

Si on avait choisi un temps de maintien $T$ inférieur à $G(A)$, on aurait fermé l'alerte
\emph{avant} l'éclair suivant — c'est un incident.

Le \textbf{risque empirique} pour un $T$ donné est juste la fraction d'alertes où $G(A) > T$.

Sur le train, à 10 \% de tolérance, $T^*(10\%) = 25$ min (gain 5 min). À 5 \%, c'est 28 min
(gain 2 min). \textbf{À 1 \%, c'est exactement 30 minutes}.

\textbf{Découverte} : la règle Météorage des 30 minutes correspond \emph{exactement} à
1 \% de risque sur les données train. Elle n'est pas un choix arbitraire — elle est
statistiquement calibrée.
\fg{}
\end{dire}

\begin{compr}
\textbf{Non-paramétrique} = sans modèle, sans hypothèse de loi.
On utilise juste les données telles quelles.

\textbf{$G(A) = $ plus grand silence intra-alerte} : pour une alerte avec les éclairs aux
instants $t_1, t_2, \dots, t_n$, on calcule les gaps $\Delta_i = t_{i+1} - t_i$ et on prend
le plus grand. Ça nous dit \og quel est l'intervalle minimum qu'il faut tolérer pour ne pas
couper l'alerte trop tôt ? \fg{}

\textbf{La logique} : si je pose la règle \og lever après $T$ min de silence \fg{}, et qu'une
alerte a $G(A) = 25$ min, alors avec $T = 20$ je rate l'alerte (le suivant arrive avant T).
Avec $T = 30$ je suis safe.

\textbf{Pourquoi 1 \% = 30 min} : ça veut dire que dans 99 \% des alertes du train, le plus grand
silence intra-alerte est inférieur ou égal à 30 min. C'est la justification statistique de la
règle Météorage.
\end{compr}

\begin{piege}
\textbf{Q : Et si on intégrait les inter-alertes (gap > 30 min) ?}

R : Bonne remarque. Par construction des alertes, on ne peut pas observer ces gaps directement
intra-alerte. Mais on pourrait extraire les gaps inter-alerte, c'est-à-dire les périodes entre
la fin d'une alerte et le début de la suivante. C'est une perspective qu'on a notée dans
les pistes d'amélioration.
\end{piege}

\begin{transit}
\og Maintenant on passe à l'évaluation sur les vraies données nouvelles : eval 2023-25. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 14 — Évaluation sur eval 2023-25}

\begin{dire}
\og On évalue le modèle sur le jeu d'évaluation 2023-25 — 973 alertes inédites, 17~037 CG.

Mais on change le critère de mesure : on adopte le \textbf{critère 3 km du protocole officiel
du Data Battle}.

Pourquoi ? Parce que le vrai risque pour un aéroport n'est pas qu'un éclair tombe dans 20 km,
c'est qu'il tombe dans 3 km du tarmac — c'est là que travaillent le personnel sol, le ravitailleur,
les chargeurs. À 15 km, ça ne change rien aux opérations.

Un \textbf{incident} dans ce critère = un éclair à moins de 3 km de l'aéroport tombant
\emph{après} la fin d'alerte qu'on a prédite.

On distingue \textbf{CG} (nuage-sol — l'éclair qui touche le sol, dangereux) et \textbf{IC}
(intra-nuage — qui reste dans le nuage, non dangereux). Seuls les CG comptent dans le risque.

Volumes : 973 alertes, 17~037 CG analysables, et \textbf{385 CG dangereux à moins de 3 km}.
C'est le dénominateur du risque.
\fg{}
\end{dire}

\begin{compr}
\textbf{Pourquoi 3 km et pas 20 km ?} 20 km c'est le rayon où Météorage détecte les éclairs
et déclenche l'alerte. Mais le risque physique pour les humains au sol se limite aux
quelques kilomètres autour de l'aéroport. 3 km c'est la zone réellement dangereuse —
chiffre donné par Sébastien de Météorage dans l'énoncé.

\textbf{CG vs IC} : un CG, c'est l'éclair classique qui touche le sol — il peut foudroyer.
Un IC reste dans le nuage et ne descend pas. Les IC ne sont pas dangereux pour les
opérations au sol, donc on ne les compte pas dans le risque opérationnel.

\textbf{Eval 2023-25} = données que le modèle n'a jamais vues à l'entraînement
(qui s'est fait sur 2016-2022). C'est la vraie validation.
\end{compr}

\begin{piege}
\textbf{Q : Et pourquoi ne pas avoir entraîné sur 3 km au lieu de 20 km ?}

R : L'entraînement est sur le \emph{temps entre éclairs} (modèle de survie), pas sur la
distance. C'est la mesure du risque qui change : on utilise les mêmes prédictions $T_q$,
mais on compte différemment les erreurs. Le critère 3 km est imposé par le protocole
officiel — c'est lui qui définit ce qui est un incident.
\end{piege}

\begin{transit}
\og Voyons concrètement comment le modèle fonctionne en mode opérationnel. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 15 — Le modèle en mode opérationnel}

\begin{dire}
\og Le modèle fonctionne en trois étapes, à chaque éclair observé.

\textbf{Étape 1 — Observation} : un CG est détecté en zone 20 km autour de l'aéroport.

\textbf{Étape 2 — Prédiction} : le modèle calcule $T_q(X)$ en fonction du contexte de cet éclair :
le rythme passé de l'orage, la distance, l'intensité, la saison. C'est notre temps d'attente
recommandé en minutes.

\textbf{Étape 3 — Décision} : si pendant $T_q$ minutes \emph{aucun nouveau CG n'arrive},
on lève l'alerte. Si un nouveau CG arrive avant $T_q$, le compteur est remis à zéro et on
recommence avec le nouveau contexte.

C'est exactement la règle Météorage actuelle (30 min fixe), mais avec un \textbf{$T_q$ adaptatif}
au contexte de chaque orage.
\fg{}
\end{dire}

\begin{compr}
\textbf{Adaptatif} = au lieu d'avoir un timer fixe de 30 min identique pour tous les orages,
on a un timer qui change selon ce qu'on observe.

Exemple : un orage très calme avec un éclair toutes les 8 min → le modèle pourrait
prédire $T_q = 15$ min (orage qui se calme). Un orage très dense avec un éclair toutes
les 30 secondes → le modèle prédirait $T_q$ proche de 30 min (orage actif).

\textbf{Reset} : c'est la même mécanique que la baseline. Tu pars d'un timer, et s'il
y a un nouvel éclair avant la fin, tu redémarres avec le nouveau contexte (qui peut
être différent du précédent !).
\end{compr}

\begin{transit}
\og Et voici les résultats sur eval. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 16 — Résultats sur eval : protocole officiel Data Battle}

\begin{dire}
\og On évalue avec exactement la procédure du jury, reproduite dans notre notebook
\texttt{Evaluation\_databattle\_weibull.ipynb}.

Concrètement, à chaque CG de l'eval, on émet une prédiction au format officiel
\texttt{predictions.csv} avec trois champs :
\begin{itemize}
    \item la date prédite de fin d'alerte = date du CG + $T_{0{,}95}$ calibré ;
    \item un score de \textbf{confidence} = $S(30 \mid X)$, la probabilité prédite par le modèle
    qu'aucun éclair ne suive dans les 30 minutes — donc que ce CG soit le dernier de l'alerte.
\end{itemize}

Nous présentons \textbf{deux mesures complémentaires} du risque, toutes deux sur le critère
CG seul <3 km (les IC ne sont pas dangereux au sol).

\textbf{Mesure 1 — 1 décision par alerte au dernier CG observé} (toutes les 1\,352 alertes
évaluées) :
\begin{itemize}
    \item À $q = 0{,}85$ : 0 \% de risque CG, \textbf{388 h} de gain
    \item À $q = 0{,}95$ : 0 \% de risque CG, \textbf{330 h} de gain
    \item À $q = 0{,}99$ : 0 \% de risque CG, 269 h de gain
\end{itemize}

\textit{Le 0 \% n'est pas un mérite du modèle} — c'est une conséquence de la définition d'une
alerte Météorage : aucun CG n'arrive plus de 30 min après le dernier, sinon une nouvelle alerte
démarre. Le modèle exploite cette structure pour lever 20 min plus tôt en moyenne.

\textbf{Mesure 2 — Sweep $\theta$ strict} (plusieurs prédictions par alerte, le jury filtre par
confidence) :
\begin{itemize}
    \item À $\theta = 0{,}85$ : 7,53 \% de risque CG, 118 h sur 140 alertes filtrées
    \item À $\theta = 0{,}95$ : 5,45 \%, 60 h sur 68 alertes
    \item À $\theta = 0{,}99$ : 2,86 \% (11/385), 26 h sur 26 alertes — meilleur risque
\end{itemize}

Cette lecture simule l'incertitude opérationnelle : si le jury filtre strictement par confidence,
le risque CG devient non-trivial mais le gain reste modeste.
\fg{}
\end{dire}

\begin{compr}
\textbf{Confidence $S(30 \mid X)$} : c'est la probabilité que le modèle donne au fait que
\emph{plus aucun éclair n'arrive dans les 30 min}. Si cette proba est élevée, le modèle te dit
\og je suis très confiant que cet éclair est le dernier \fg{}.

\textbf{Pourquoi 2 mesures ?}
\begin{itemize}
    \item \textbf{Mesure 1 (1 décision/alerte au last CG)} = ce qu'on déploierait en production.
    À chaque alerte, on attend $T_q$ minutes après le dernier CG observé. Si rien ne suit, on lève.
    Le 0 \% CG est trivial mais factuel : impossible de rater un CG par construction de l'alerte.
    \item \textbf{Mesure 2 (sweep $\theta$ strict)} = ce que le jury évalue avec un fichier
    \texttt{predictions.csv}. On émet une prédiction à chaque CG avec une confidence
    $S(30|X)$. Le jury filtre par $\theta$. Comme la confidence est imparfaite, des CG en milieu
    d'alerte peuvent passer θ et provoquer un \og lift en avance \fg{} qui rate des CG ultérieurs.
\end{itemize}

\textbf{Les deux sont valides et complémentaires :}
\begin{itemize}
    \item Mesure 1 donne le potentiel opérationnel \emph{réel} (330 h gagnées sans incident).
    \item Mesure 2 quantifie le \og coût d'incertitude \fg{} si on suit strictement le protocole.
\end{itemize}
\end{compr}

\begin{piege}
\textbf{Q : Vous n'atteignez pas $R < 2 \%$ avec le sweep $\theta$. C'est un échec ?}

R : Non, c'est honnête sur la mesure stricte. Avec la confidence $S(30 \mid X)$ seule, on
plafonne à 2,86 \% sur CG <3 km à $\theta=0{,}99$. Mais sur la mesure principale (1 décision
par alerte au dernier CG), on est à 0 \% CG / 330 h gagnées. La différence quantifie le coût
de l'incertitude opérationnelle — pour la résorber, on entraînerait un classifieur secondaire
sur la cible \og ce CG est-il le dernier ? \fg{} avec plusieurs features ($T_q$, rolling\_gap,
cg\_rank). C'est une perspective claire.

\textbf{Q : Pourquoi 6 631 h dans le Streamlit alors ?}

R : Le Streamlit affichait jusqu'à hier un \og gain dynamique \fg{} qui somme $(30 - T_q)$ sur
les 17~037 CG. C'est mathématiquement gonflé : on ne lève qu'une seule fois par alerte (au
dernier CG). On a corrigé l'app pour afficher le gain opérationnel par alerte, cohérent
avec les 26 h du protocole.
\end{piege}

\begin{transit}
\og Voyons maintenant des cas concrets où le modèle a sous-estimé un gap. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 17 — Cas concrets pédagogiques (démo Streamlit)}

\begin{dire}
\og Cette slide montre 5 exemples concrets parmi 37 où le modèle a \textbf{sous-estimé}
le gap jusqu'au prochain CG. C'est une \textbf{mesure dynamique pédagogique} : on vérifie
la prédiction à \emph{chaque} CG, et non pas la décision finale par alerte.

Sur \texttt{Pise\_1319}, le modèle a prédit qu'aucun CG n'arriverait avant 2,75 min après
le CG de référence. En réalité, le prochain CG est arrivé 35,78 min plus tard, à 1,87 km
de l'aéroport — un CG dangereux. Si on avait levé à 2,75 min, l'aéroport aurait été en
opérations sol pendant 33 min avant qu'un éclair dangereux frappe.

\textbf{Important} : ces 37 cas ne sont \emph{pas} le risque opérationnel mesuré par
le protocole. Le risque réel reste à 3,6 \% (sweep $\theta$) à 0,6 \% (oracle).
Ces cas sont là pour la \textbf{pédagogie} — pour qu'on voie où le modèle se trompe
intermédiairement. Tout est visualisable dans la démo Streamlit avec une timeline
graphique de chaque incident.
\fg{}
\end{dire}

\begin{compr}
\textbf{Mesure dynamique vs opérationnelle} :
\begin{itemize}
    \item \textbf{Dynamique} = on vérifie la prédiction du modèle à \emph{chaque} CG.
    Sur 17~037 CG, le modèle se trompe 37 fois (= 9,61 \% des 385 CG dangereux).
    Mais en pratique, beaucoup de ces \og erreurs \fg{} sont sur des CG en plein milieu
    de l'alerte, où on n'aurait jamais levé de toute façon parce qu'un autre CG est arrivé
    après.

    \item \textbf{Opérationnelle / protocole} = on regarde uniquement la décision finale par
    alerte (= la dernière fois que le timer s'écoule). Là, le risque est de 3,6 \% ou 0,6 \%.
\end{itemize}

La mesure dynamique sert à \textbf{comprendre} les erreurs, pas à mesurer le risque réel.
\end{compr}

\begin{piege}
\textbf{Q : Pourquoi 37 erreurs mais seulement 26 h de gain ? Ce n'est pas incohérent ?}

R : Non, parce que les 37 erreurs sont sur des CG \emph{intermédiaires} qui n'ont pas
déclenché de levée en pratique (un autre CG est arrivé avant la fin du timer).
Le gain de 26 h se calcule différemment : c'est le temps gagné sur la \emph{dernière}
décision par alerte. Les deux mesures sont complémentaires : la dynamique compte les
sous-estimations, l'opérationnelle compte le gain final.
\end{piege}

\begin{transit}
\og On vérifie maintenant nos résultats avec l'approche non-paramétrique 3 km. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 18 — Risque empirique non-paramétrique 3 km}

\begin{dire}
\og On répète la vérification empirique non-paramétrique, mais cette fois sur le critère 3 km.

Pour chaque CG de l'eval, on calcule l'écart $\Delta_i$ jusqu'au prochain CG et sa distance.
Pour un seuil $T$ fixe — la règle \og lever après $T$ min de silence pour tous les
orages \fg{} — combien de CG dangereux seraient ratés ?

Résultats : à 10 \% de tolérance, $T^* = 5$ min (gain 25 min vs baseline). À 5 \%, $T^* = 9$ min
(gain 21 min). À 1 \%, c'est plus de 30 min — \textbf{non atteignable}.

Pour le critère 3 km, la baseline 30 min reste donc nécessaire si on veut absolument
1 \% de risque sur les CG dangereux.

C'est une validation indépendante : sans modèle, on confirme que l'objectif 1 \% sur 3 km
demande de maintenir la baseline.
\fg{}
\end{dire}

\begin{compr}
\textbf{Non-paramétrique 3 km} : c'est la même logique que slide 13, mais en comptant
uniquement les éclairs $<3$ km comme \og dangereux \fg{}.

\textbf{Lecture} : $T^*(5\%) = 9$ min veut dire \og si je pose la règle \emph{toute alerte
est levée 9 min après le dernier CG}, alors 5 \% des alertes auront un CG dangereux qui
arrive après cette levée \fg{}.

\textbf{Pourquoi 1 \% non atteignable ?} Parce que sur eval, il existe des alertes où
$G(A) > 30$ min avec un CG dangereux. Donc même la baseline 30 min ne couvre pas tout.
\end{compr}

\begin{transit}
\og Et tout ça est interactif dans la démo. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 19 — Démo Streamlit interactive}

\begin{dire}
\og La démo Streamlit, lancée par \texttt{streamlit run app.py}, permet d'inspecter
chaque cas concret.

Dans la sidebar, on choisit le niveau de confiance $q$. Sur le tableau de bord :
\begin{itemize}
    \item KPI globaux : nombre de CG évalués, levées prématurées, incidents, risque réel.
    \item Gain opérationnel \emph{par alerte} (le bon chiffre, cohérent avec le protocole).
    \item Comparaison avec la baseline 30 min.
    \item Liste des incidents avec sélecteur pour ouvrir la \textbf{timeline visuelle}.
    \item Risque empirique non-paramétrique en bas.
    \item Synthèse par aéroport.
\end{itemize}

Sur la timeline d'un incident, on voit :
\begin{itemize}
    \item \textbf{étoile rouge} = CG dangereux $<3$ km ;
    \item \textbf{ligne verte} = notre levée prédite à $T_q$ ;
    \item \textbf{ligne rouge pointillée} = arrivée du prochain CG ;
    \item \textbf{bande rose} = fenêtre \og off-alerte \fg{}.
\end{itemize}

C'est l'outil que le jury peut utiliser pour explorer librement.
\fg{}
\end{dire}

\begin{compr}
\textbf{Streamlit} = framework Python pour faire un dashboard web rapidement.

L'idée : le jury peut bouger le slider $q$ pour voir comment le compromis gain/risque
évolue. C'est une démonstration interactive, plus parlante qu'un tableau statique.

\textbf{Si le jury demande de voir la démo} : tu lances \texttt{streamlit run app.py}
dans un terminal, ça ouvre une page web locale. Tu sélectionnes $q = 0{,}95$, tu cliques
sur \og Lancer l'évaluation \fg{}, et tu navigues dans les incidents.
\end{compr}

\begin{transit}
\og Pour conclure, voyons les limites et perspectives. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 20 — Limites et perspectives}

\begin{dire}
\og \textbf{Limites assumées de notre travail :}
\begin{itemize}
    \item Les données sont censurées à 30 min par construction (on ne voit pas les gaps
    intra-alerte supérieurs à 30 min).
    \item Pas de données météo externes : pas de vent, pas de pression, pas d'humidité.
    \item Calibration sur le test (idéalement il faudrait 3 splits : train / calibration / test).
    \item Le modèle est statique : il faudrait un re-fit annuel pour absorber la dérive climatique.
    \item Pour le protocole officiel, la confidence $S(30 \mid X)$ ne suffit pas à atteindre
    $R < 2 \%$ — il faudrait un classifieur secondaire dédié.
\end{itemize}

\textbf{Prochaines étapes :}
\begin{itemize}
    \item Intégrer la météo via l'API de Météo France ou ECMWF.
    \item Tester l'option \texttt{ancillary} de lifelines (le paramètre $k$ variable selon $X$).
    \item Pipeline de re-calibration mensuelle automatisé.
    \item Validation A/B en production avant déploiement.
    \item \textbf{Classifieur dédié \og dernier CG \fg{}} pour améliorer la confidence
    et atteindre les 2 \% du protocole.
\end{itemize}
\fg{}
\end{dire}

\begin{compr}
\textbf{Censure à droite} : on l'a déjà dit — on ne voit pas les gaps $>30$ min intra-alerte.

\textbf{Dérive climatique} : le climat change. Un modèle entraîné sur 2016-2022 peut devenir
moins bon en 2030 si la fréquence ou l'intensité des orages change. Le re-fit annuel
permet de maintenir la qualité.

\textbf{Validation A/B} : en production, tu ne déploies pas direct. Tu mets le modèle en
shadow mode (il prédit mais ne lève rien) pendant quelques mois pour comparer ses
décisions à la baseline. Si les chiffres sont bons, tu déploies pour de bon.
\end{compr}

\begin{piege}
\textbf{Q : Pourquoi avoir limité $T_q$ à 30 min ? Vous brident le modèle.}

R : C'est un choix de \og safety net \fg{}. La baseline Météorage à 30 min est statistiquement
calibrée pour 1 \% de risque sur le critère 20 km. Si le modèle propose un $T_q$ supérieur à
30 min, on préfère garder 30 min — on ne veut jamais être \emph{moins prudent} que la
baseline. C'est une décision opérationnelle, pas mathématique.
\end{piege}

\begin{transit}
\og En résumé. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 21 — En résumé}

\begin{dire}
\og Trois grands points pour conclure.

\textbf{Ce qu'on a fait :}
\begin{itemize}
    \item Un \textbf{Weibull AFT} entraîné par maximum de vraisemblance qui gère la censure
    nativement, avec 17 variables contextuelles dont le rythme passé concentre 57 \% du
    pouvoir prédictif.
    \item Une \textbf{calibration empirique robuste} (winsorization + monotonicité) pour
    aligner le risque annoncé sur le risque réel.
    \item Une \textbf{vérification non-paramétrique indépendante} qui confirme les résultats.
    \item Une \textbf{reproduction fidèle du protocole officiel Data Battle} (sweep $\theta$
    sur \texttt{predictions.csv}).
\end{itemize}

\textbf{Ce qu'on gagne sur eval 2023-25 (1\,352 alertes, 385 CG <3 km) :}

Mesure principale (1 décision/alerte au last CG, toutes alertes évaluées) :
\begin{itemize}
    \item À $q = 0{,}85$ : \textbf{0 \% de risque CG, 388 h gagnées}.
    \item À $q = 0{,}95$ : \textbf{0 \% de risque CG, 330 h gagnées}.
    \item À $q = 0{,}99$ : 0 \% de risque CG, 269 h gagnées.
    \item Baseline Météorage 30 min : 0 \% / 0 h (référence).
\end{itemize}

Mesure alternative (sweep $\theta$ strict — incertitude opérationnelle simulée) :
\begin{itemize}
    \item À $\theta = 0{,}99$ : 2,86 \% (11/385) / 26 h sur 26 alertes.
    \item À $\theta = 0{,}85$ : 7,53 \% / 118 h sur 140 alertes.
    \item Démo Streamlit pour visualiser chaque cas concret.
\end{itemize}

\textbf{Validation indépendante :} risque empirique non-paramétrique cohérent avec le
modèle paramétrique. Démo interactive permet au jury d'inspecter chaque cas.
\fg{}
\end{dire}

\begin{compr}
\textbf{Les 3 chiffres à retenir pour le jury :}
\begin{enumerate}
    \item \textbf{0 \% CG / 330 h gagnées} à $q=0{,}95$ — mesure principale (1 décision /
    alerte). C'est ce qu'on déploierait en production.
    \item \textbf{2,86 \% CG / 26 h} à $\theta=0{,}99$ — mesure sweep $\theta$ strict
    (incertitude opérationnelle simulée).
    \item \textbf{0 \%} : baseline Météorage 30 min (= référence parfaite par construction).
\end{enumerate}

Le message : sur la mesure principale, le modèle est aussi sûr que la baseline avec 330 h
gagnées sur 1\,352 alertes. Sur la mesure stricte, on quantifie le coût d'incertitude — un
classifieur \og dernier CG \fg{} dédié résorberait cet écart.
\end{compr}

\begin{transit}
\og Merci. \fg{}
\end{transit}

% ==========================================================================
\section{Slide 22 — Merci}

\begin{dire}
\og Merci pour votre attention.

Le code, le rapport, la présentation et la démo Streamlit sont à disposition pour les
questions. \fg{}
\end{dire}

\begin{compr}
Si silence du jury : \og On reste à disposition pour les questions. \fg{}
Si question hostile : reste calme, reformule la question, prends 2 secondes pour réfléchir,
puis réponds. Si tu ne sais pas, dis-le.
\end{compr}

% ==========================================================================
\section{Vue globale du code — le pipeline en 7 étapes}
\label{sec:code}

\textbf{Tout le pipeline est centralisé dans \texttt{meteorage\_model.py}} (202 lignes,
réutilisé par les notebooks et par Streamlit). Le code est volontairement modulaire :
chaque étape est une fonction pure, testable indépendamment.

\begin{tcolorbox}[colback=gray!5, colframe=gray!50!black, title=\textbf{Le pipeline en une ligne}]
\texttt{cg = load\_train(path)} $\to$ \texttt{add\_features(cg)} $\to$ \texttt{add\_target(cg)} $\to$
\texttt{build\_model\_matrix(cg, scaler, [], fit\_scaler=True)} $\to$ \texttt{fit\_weibull(fit\_df)} $\to$
\texttt{compute\_robust\_calibration(aft, fit\_df)} $\to$ \texttt{predict\_T\_q\_calibrated(aft, X, scaling, q)}
\end{tcolorbox}

\subsection{Étape 1 — \texttt{load\_train} : filtrage et identifiants uniques}

\begin{lstlisting}[caption={Chargement et nettoyage. Le filtre Pise 2016 vient d'une différence de système d'enregistrement.}]
def load_train(path: str) -> pd.DataFrame:
    df = pd.read_csv(path, parse_dates=['date'])
    alerts = df.dropna(subset=['airport_alert_id']).copy()
    # On rend l'ID unique en concatenant aeroport + numero d'alerte
    alerts['airport_alert_id'] = alerts['airport_alert_id'].astype(int).astype(str)
    alerts['airport_alert_id'] = alerts['airport'] + '_' + alerts['airport_alert_id']
    # Exclure Pise 2016 (systeme d'enregistrement different)
    alerts['year'] = alerts['date'].dt.year
    mask_pise_2016 = (alerts['airport'] == 'Pise') & (alerts['year'] == 2016)
    alerts = alerts.loc[~mask_pise_2016].copy()
    # Garder uniquement les CG (nuage-sol, icloud=False)
    cg = alerts.loc[~alerts['icloud']].copy()
    cg = cg.sort_values(['airport', 'airport_alert_id', 'date']).reset_index(drop=True)
    return cg
\end{lstlisting}

\textbf{Points-clés} :
\begin{itemize}[leftmargin=*]
    \item \texttt{dropna(subset=['airport\_alert\_id'])} : on ne garde que les éclairs dans une alerte (= zone 20 km).
    \item Concat \texttt{aeroport\_id} : sinon \og 530 \fg{} peut désigner 5 alertes différentes (une par aéroport).
    \item Exclusion Pise 2016 : justifiée techniquement (différent enregistrement IC).
    \item \texttt{icloud=False} : on ne garde que les CG, parce que le modèle prédit le temps jusqu'au prochain \emph{CG} (= ce qui ferme l'alerte).
\end{itemize}

\subsection{Étape 2 — \texttt{add\_features} : les 17 variables contextuelles}

\begin{lstlisting}[caption={Construction des features. Toutes utilisent \texttt{groupby + transform} pour rester intra-alerte.}]
def add_features(cg):
    cg = cg.copy()
    # 1) Saisonnalite cyclique (sin/cos)
    cg['month_sin'] = np.sin(2 * np.pi * cg['date'].dt.month / 12)
    cg['month_cos'] = np.cos(2 * np.pi * cg['date'].dt.month / 12)
    cg['hour_sin']  = np.sin(2 * np.pi * cg['date'].dt.hour / 24)
    cg['hour_cos']  = np.cos(2 * np.pi * cg['date'].dt.hour / 24)

    # 2) Rythme passe (intra-alerte avec groupby)
    grp = cg.groupby(['airport', 'airport_alert_id'], sort=False)
    cg['time_since_start'] = (cg['date'] - grp['date'].transform('first')).dt.total_seconds() / 60
    cg['prev_gap']         = grp['date'].diff().dt.total_seconds().div(60).fillna(0)
    cg['cg_rank']          = grp.cumcount() + 1
    cg['n_cg_alert']       = grp['cg_rank'].transform('max')
    cg['rolling_gap_3']    = grp['prev_gap'].transform(lambda x: x.rolling(3, min_periods=1).mean())
    cg['rolling_gap_5']    = grp['prev_gap'].transform(lambda x: x.rolling(5, min_periods=1).mean())
    cg['trend_gap']        = (cg['prev_gap'] / cg['rolling_gap_5'].replace(0, np.nan)).fillna(1)

    # 3) Distance / intensite (cumul intra-alerte avec expanding)
    cg['dist_current']  = cg['dist']
    cg['dist_cum_mean'] = grp['dist'].transform(lambda x: x.expanding().mean())
    cg['dist_diff']     = grp['dist'].diff().fillna(0)
    cg['amp_abs']       = cg['amplitude'].abs()
    cg['amp_cum_mean']  = grp['amp_abs'].transform(lambda x: x.expanding().mean())
    cg['is_negative']   = (cg['amplitude'] < 0).astype(int)
    cg['pct_neg_cum']   = grp['is_negative'].transform(lambda x: x.expanding().mean())
    return cg
\end{lstlisting}

\textbf{Pourquoi sin/cos pour mois/heure ?} L'encodage cyclique évite la fausse discontinuité
entre décembre (mois 12) et janvier (mois 1). Avec un encodage ordinaire, le modèle penserait
que ces deux mois sont \og loin \fg{}, alors qu'ils sont voisins dans le cycle annuel.

\textbf{Pourquoi \texttt{expanding} et pas \texttt{rolling} pour les cumuls de distance ?}
\texttt{expanding} accumule depuis le \emph{début de l'alerte}, ce qui capture la tendance
globale de l'orage. \texttt{rolling} fenêtre les $k$ derniers points, ce qui capture une
dynamique locale. On utilise les deux selon ce qu'on veut mesurer.

\textbf{Aucune fuite (\og leak \fg{})} : toutes les opérations sont causales — elles ne
regardent que les CG \emph{déjà arrivés}, jamais le futur. \texttt{groupby + transform}
garantit qu'on reste intra-alerte (pas de fuite inter-alerte).

\subsection{Étape 3 — \texttt{add\_target} : censure native à 30 min}

\begin{lstlisting}[caption={C'est ici qu'on encode la survie. duration et event sont les deux colonnes que lifelines attend.}]
def add_target(cg, is_last_col):
    cg = cg.copy()
    grp = cg.groupby(['airport', 'airport_alert_id'], sort=False)
    # Cible = temps jusqu'au prochain CG
    cg['target'] = grp['time_since_start'].shift(-1) - cg['time_since_start']
    cg['is_last'] = cg['cg_rank'] == cg['n_cg_alert']
    cg['duration'] = cg['target'].copy()
    cg['event'] = (~cg['is_last']).astype(int)
    # CENSURE : pour le dernier CG d'une alerte, on n'observe pas le prochain CG.
    # On sait juste qu'aucun CG n'est arrive pendant 30 min.
    cg.loc[cg['is_last'], 'duration'] = 30.0
    return cg
\end{lstlisting}

\textbf{La ligne magique} : \texttt{cg.loc[cg['is\_last'], 'duration'] = 30.0}. Pour le dernier
CG d'une alerte, on ne sait pas combien de temps il a fallu avant le \emph{vrai} prochain CG
(il y en a un, plus tard, mais ça démarre une nouvelle alerte). On sait juste qu'il n'y a rien
eu pendant au moins 30 min. C'est exactement une observation \textbf{censurée à droite} :
\texttt{event=0}, \texttt{duration=30}. lifelines intègre ça nativement dans la vraisemblance.

\subsection{Étape 4 — \texttt{build\_model\_matrix} : standardisation + dummies aéroport}

\begin{lstlisting}[caption={Préparation finale. fit\_scaler=True uniquement sur train.}]
def build_model_matrix(cg, scaler, apt_cols, fit_scaler=False):
    # On filtre les alertes avec moins de 3 CG (pas assez de contexte) et duration<=0
    model_df = cg[cg['n_cg_alert'] >= 3].copy()
    model_df = model_df[model_df['duration'] > 0].copy()
    for col in FEATURES:
        model_df[col] = model_df[col].replace([np.inf, -np.inf], np.nan).fillna(0)

    # 1) Standardisation des 17 features (moyenne 0, ecart-type 1)
    if fit_scaler:
        scaler.fit(model_df[FEATURES])
    model_df_sc = model_df.copy()
    model_df_sc[FEATURES] = scaler.transform(model_df[FEATURES])

    # 2) Dummies aeroport (one-hot, drop_first pour eviter colinearite)
    apt_dum = pd.get_dummies(model_df_sc['airport'], prefix='apt', drop_first=True).astype(float)
    if fit_scaler:
        apt_cols = list(apt_dum.columns)
    else:
        # En eval : on aligne les colonnes pour avoir le meme set qu'au train
        for col in apt_cols:
            if col not in apt_dum.columns:
                apt_dum[col] = 0.0
        apt_dum = apt_dum[apt_cols]

    fit_df = pd.concat([
        model_df_sc[FEATURES + ['duration', 'event']].reset_index(drop=True),
        apt_dum.reset_index(drop=True),
    ], axis=1)
    fit_df = fit_df.replace([np.inf, -np.inf], np.nan).fillna(0)
    fit_df = fit_df[fit_df['duration'] > 0]
    return model_df_sc.reset_index(drop=True), fit_df, apt_cols
\end{lstlisting}

\textbf{Standardisation} : indispensable pour qu'aucune feature ne domine artificiellement
(ex.\ \texttt{time\_since\_start} en minutes vs \texttt{is\_negative} qui vaut 0 ou 1).
\textbf{Le scaler est fit \emph{uniquement} sur train} (paramètre \texttt{fit\_scaler=True})
et utilisé en \og transform-only \fg{} sur eval — c'est la règle d'or pour éviter la fuite.

\textbf{Dummies aéroport, \texttt{drop\_first=True}} : on crée 4 colonnes binaires pour les 5
aéroports (Bastia, Biarritz, Nantes, Pise vs référence Ajaccio). \texttt{drop\_first} évite
la colinéarité parfaite (sinon les 5 colonnes somment à 1 = $\beta_0$, le modèle ne peut pas
distinguer).

\subsection{Étape 5 — \texttt{fit\_weibull} : 3 lignes pour le MLE avec censure}

\begin{lstlisting}[caption={lifelines fait tout le travail.}]
def fit_weibull(train_fit, penalizer=0.05):
    aft = WeibullAFTFitter(penalizer=penalizer, l1_ratio=0.0)
    aft.fit(train_fit, duration_col='duration', event_col='event')
    return aft
\end{lstlisting}

\textbf{Ce que ces 3 lignes font sous le capot :}
\begin{itemize}[leftmargin=*]
    \item Maximisation numérique (Newton-Raphson) de la log-vraisemblance avec censure.
    \item Régularisation L2 ridge avec \texttt{penalizer=0.05}. Évite l'overfit, plus stable
    numériquement avec 17+ features.
    \item \texttt{l1\_ratio=0.0} : 100 \% ridge, pas de sparsity Lasso (on garde tous les
    coefficients).
\end{itemize}

\textbf{Sortie} : un objet \texttt{aft} qui expose \texttt{predict\_percentile(X, p=1-q)} pour
calculer $T_q$, \texttt{predict\_survival\_function(X, times=[30])} pour $S(30|X)$, etc.

\subsection{Étape 6 — \texttt{compute\_robust\_calibration} : winsorize + monotonicité}

\begin{lstlisting}[caption={Calibration ex-post pour aligner risque annonce et risque reel.}]
def compute_robust_calibration(aft, fit_df, q_levels=[0.90, 0.95, 0.99]):
    evt = fit_df[fit_df['event'] == 1].copy()
    y_real = evt['duration'].values

    c_q_winsorized = {}
    for q in q_levels:
        # Tq predit par Weibull pour le quantile q
        Tq = aft.predict_percentile(evt, p=1.0 - q).values
        # Residu = ratio entre temps reel et temps predit
        residuals = y_real / np.where(Tq > 1e-9, Tq, 1e-9)
        # Winsorize au P95 pour neutraliser les outliers
        p95 = np.quantile(residuals, 0.95)
        residuals_wins = np.minimum(residuals, p95)
        # Coefficient c_q = quantile q des residus winsorises
        c_q_winsorized[q] = float(np.quantile(residuals_wins, q))

    # Imposer la monotonicite : c_99 >= c_95 >= c_90
    scaling = {}
    prev_c = 0.0
    for q in sorted(q_levels):
        c_q_mono = max(c_q_winsorized[q], prev_c)
        scaling[q] = c_q_mono
        prev_c = c_q_mono
    return scaling
\end{lstlisting}

\subsection{Étape 7 — Prédiction calibrée et confidence}

\begin{lstlisting}[caption={Utilisation en production. Cap a 30 min = filet de securite operationnel.}]
def predict_T_q_calibrated(aft, X, scaling, q):
    Tq_raw = aft.predict_percentile(X, p=1.0 - q).values
    return scaling[q] * Tq_raw

# Pour la confidence (utilisee dans le protocole officiel) :
# confidence = S(30 | X) = exp(-(30 / lambda(X))^k)
survival_df = aft.predict_survival_function(X, times=[30.0])
confidence = survival_df.iloc[0].values

# Cap operationnel a 30 min : on ne veut jamais lever PLUS TARD que la baseline
Tq_cap = np.clip(scaling[q] * Tq_raw, 0, 30)
\end{lstlisting}

% ==========================================================================
\section{Q/R techniques anticipées (sur le code et le modèle)}
\label{sec:qa-tech}

\subsection*{Sur la bibliothèque et l'algorithme}

\begin{piege}
\textbf{Q : Pourquoi \texttt{lifelines} et pas une implémentation maison ?}

R : \texttt{lifelines} est la référence Python pour l'analyse de survie (Cameron Davidson-Pilon).
Implémentation maison : on aurait dû écrire la log-vraisemblance avec censure, le gradient,
gérer les contraintes ($\lambda > 0, k > 0$), et l'optimisation Newton-Raphson. lifelines
fait tout ça avec une API propre et testée.
\end{piege}

\begin{piege}
\textbf{Q : Comment lifelines gère la censure dans la vraisemblance ?}

R : Pour une observation $(t_i, \delta_i, X_i)$ avec $\delta_i \in \{0, 1\}$ :
\begin{itemize}
    \item Si $\delta_i = 1$ (événement observé) : contribution = $\log f(t_i \mid X_i)$ (densité)
    \item Si $\delta_i = 0$ (censure) : contribution = $\log S(t_i \mid X_i)$ (survie)
\end{itemize}
La log-vraisemblance totale est la somme. lifelines optimise par Newton-Raphson sur les
paramètres $(\beta, \log k)$ — le $\log$ assure que $k > 0$ implicitement.
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi \texttt{penalizer=0.05} et pas 0 ou 0.1 ?}

R : Choix empirique. À 0 (pas de pénalisation), le modèle est plus instable numériquement avec
17 features + 4 dummies. À 0.1, il est trop régularisé et perd un peu de pouvoir prédictif
(C-index baisse). 0.05 est un bon compromis trouvé par cross-validation rapide. C'est une
pénalité ridge $\lambda \sum \beta_j^2$ ajoutée à la log-vraisemblance.
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi pas un gradient boosting (XGBoost / LightGBM) ?}

R : Trois raisons :
\begin{enumerate}
    \item \textbf{La censure} : les GBM classiques ne savent pas gérer nativement les données
    censurées (un éclair où on sait juste qu'il n'y a rien eu pendant 30 min). Weibull AFT oui.
    \item \textbf{Les quantiles analytiques} : on a la formule $T_q = \lambda \cdot (-\ln q)^{1/k}$.
    Un GBM nous donnerait juste une médiane.
    \item \textbf{Interprétabilité} : on lit directement l'effet de chaque variable via $\beta_j$.
    Un GBM est plus boîte noire.
\end{enumerate}
On a quand même testé un GBM en hybride (cellule 16 du notebook \texttt{weibull\_final}) à
titre diagnostic — Weibull AFT calibré reste meilleur sur notre C-index.
\end{piege}

\begin{piege}
\textbf{Q : Comment \texttt{predict\_percentile(p)} fonctionne ?}

R : Pour un Weibull paramétré par $\lambda(X)$ et $k$ :
\[
S(t \mid X) = \exp(-(t/\lambda)^k) = p \quad \Rightarrow \quad t = \lambda(X) \cdot (-\ln p)^{1/k}
\]
\texttt{predict\_percentile(X, p)} retourne ce $t$. Donc pour avoir le quantile $q$ (qui couvre
$q$ \% des cas), on appelle avec \texttt{p = 1 - q}. C'est pour ça qu'on a $p = 1 - q$ partout.
\end{piege}

\subsection*{Sur les features et la cible}

\begin{piege}
\textbf{Q : Pourquoi exclure les alertes avec \texttt{n\_cg\_alert < 3} ?}

R : Pour les features de rythme (rolling\_gap\_3, rolling\_gap\_5, trend\_gap), il faut au
moins 3 points pour avoir un signal exploitable. Avec 1 ou 2 CG, les rolling sont dégénérés
(NaN ou identiques à \texttt{prev\_gap}). On préfère exclure ces cas en entraînement —
ils représentent moins de 5 \% des alertes et ajoutent du bruit.
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi pas de \texttt{prev\_gap} pour le tout premier CG ?}

R : Effectivement le premier CG n'a pas de prédécesseur. On met \texttt{prev\_gap = 0} et
\texttt{cg\_rank = 1}. Ces cas sont rares (1/N par alerte) et leur contribution au fit est
faible. Une alternative serait d'utiliser le gap inter-alerte (temps depuis la fin de l'alerte
précédente), mais on ne l'a pas exploité ici.
\end{piege}

\begin{piege}
\textbf{Q : Comment gérer \texttt{duration = 0} ? (deux éclairs simultanés)}

R : On les exclut avec \texttt{model\_df[model\_df['duration'] > 0]} dans
\texttt{build\_model\_matrix}. Causes possibles : précision temporelle de Météorage à la
seconde, deux éclairs détectés au même horodatage. Les exclure évite la singularité dans le
log-vraisemblance ($\log(0) = -\infty$).
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi \texttt{drop\_first=True} pour les dummies aéroport ?}

R : Pour éviter la colinéarité parfaite. Avec 5 aéroports, créer 5 dummies (\texttt{apt\_Ajaccio},
\texttt{apt\_Bastia}, ...) donne 5 colonnes qui somment toujours à 1. Combiné à
l'intercept $\beta_0$, ça donne une matrice de design singulière. \texttt{drop\_first} retire
une colonne (Ajaccio devient la \og référence \fg{}), les coefficients $\beta$ des 4 autres
mesurent l'écart à Ajaccio.
\end{piege}

\subsection*{Sur le scaler et l'eval}

\begin{piege}
\textbf{Q : Comment le scaler est-il appliqué en eval ?}

R : On fit le scaler une fois sur train (\texttt{fit\_scaler=True}), il apprend la moyenne et
l'écart-type de chaque feature \emph{sur train}. En eval, on l'utilise en mode transform
uniquement (\texttt{fit\_scaler=False}) — il applique les mêmes paramètres train aux données
eval. C'est la règle d'or pour éviter la fuite : \textbf{aucune statistique du test ne doit
fuiter dans le fit}.
\end{piege}

\begin{piege}
\textbf{Q : Et si un aéroport apparaît en eval mais pas en train ?}

R : Géré par le code \texttt{build\_model\_matrix} :
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
for col in apt_cols:
    if col not in apt_dum.columns:
        apt_dum[col] = 0.0
apt_dum = apt_dum[apt_cols]
\end{lstlisting}
Si un aéroport nouveau apparaît, sa dummy n'existe pas dans \texttt{apt\_cols} (qui vient
du train), donc il devient implicitement la \og référence \fg{} (Ajaccio dans notre cas).
C'est imparfait mais ça ne crash pas. Sur eval 2023-25, les 5 aéroports sont les mêmes
qu'en train, donc ce cas n'arrive pas.
\end{piege}

\begin{piege}
\textbf{Q : Le scaler influence-t-il la calibration ?}

R : Non. La calibration utilise $T_q$ prédit par le modèle (déjà sur l'échelle des minutes,
pas standardisée) et les vraies durées en minutes. Le scaler intervient \emph{avant} le fit
(sur les features X), pas sur la cible \texttt{duration}. La calibration travaille uniquement
sur des durées en minutes, donc le scaler est transparent.
\end{piege}

\subsection*{Sur la calibration}

\begin{piege}
\textbf{Q : Pourquoi winsoriser au P95 et pas un autre seuil ?}

R : Choix empirique. P95 retire les ~5 \% d'outliers extrêmes (en pratique des observations
où l'orage a dévié totalement de ce que prédit Weibull, souvent à cause d'une transition de
système orageux). Un seuil plus bas (P90) corrigerait trop fort. Un seuil plus haut (P99)
laisserait passer les outliers. P95 est le compromis standard en stats robustes.
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi $c_{99}^{\text{wins}} < c_{95}$ après winsorization ?}

R : Effet artificiel de la winsorization. Quand on tronque la queue à P95, les valeurs au-dessus
du P95 deviennent toutes égales. Le quantile $q=0.99$ \emph{après} winsorization est donc
le P95 \emph{avant} winsorization. Si Weibull surestime déjà cette partie de la queue, $c_{99}$
peut devenir plus petit que $c_{95}$. L'imposition de monotonicité ($c_q = \max(c_q, c_{q-1})$)
corrige cet artefact.
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi la calibration est-elle faite sur le test et pas sur un fold dédié ?}

R : Limite assumée. L'idéal est \textbf{3 splits} : train pour fit, calibration pour estimer $c_q$,
test pour évaluation finale. On a fait 2 splits par contrainte de volume (54k CG). En
production, on referait avec 3 splits. La winsorization protège partiellement de l'overfit
à la calibration en neutralisant les outliers.
\end{piege}

\subsection*{Sur le critère de risque et l'évaluation}

\begin{piege}
\textbf{Q : Comment le C-index est-il calculé ?}

R : Pour toute paire $(i, j)$ d'observations non-censurées dans le test, on regarde si
le modèle prédit la même hiérarchie que la réalité. \texttt{concordance\_index(durations,
predictions, events)} de lifelines fait ça en $O(N^2)$ ou avec un index spécial en $O(N \log N)$.
Notre prédiction utilisée : \texttt{aft.predict\_median(test\_df)} (la médiane prédite).
\end{piege}

\begin{piege}
\textbf{Q : Pourquoi cap à 30 min ? Vous bridez le modèle.}

R : C'est un \og safety net \fg{}. La règle Météorage 30 min est calibrée pour 1 \% de risque
sur 20 km. Si notre modèle propose $T_q > 30$ min, on préfère garder 30 min — on ne veut
\emph{jamais être moins prudent} que la baseline. Décision opérationnelle, pas mathématique.
Sans le cap, $T_{99}$ peut dépasser 90 min sur des cas extrêmes.
\end{piege}

\begin{piege}
\textbf{Q : La reproductibilité ? Random seed ?}

R : Le fit Weibull est déterministe (optimisation convexe avec pénalité ridge, point initial
fixé par lifelines). Pas besoin de seed. \texttt{predict\_percentile} est aussi déterministe.
Donc en relançant le notebook on obtient exactement les mêmes chiffres — c'est testé.
\end{piege}

\subsection*{Sur le déploiement et la production}

\begin{piege}
\textbf{Q : Combien de temps prend une prédiction ?}

R : Une prédiction $T_q(X)$ = un produit matriciel sur un vecteur de 17+4 dimensions =
quelques microsecondes. Le coût opérationnel est \emph{négligeable} — l'inférence est
1000$\times$ plus rapide qu'un appel API externe. Le bottleneck en production serait
l'agrégation Météorage (qui détecte les éclairs en temps réel).
\end{piege}

\begin{piege}
\textbf{Q : Comment vous déploieriez en production ?}

R :
\begin{enumerate}
    \item Sérialisation du modèle avec \texttt{joblib.dump(aft, scaler, scaling)} (en
    pratique \texttt{meteorage\_artifact.pkl} déjà présent dans le repo).
    \item Service Python (FastAPI ou Flask) qui reçoit le contexte courant et retourne $T_q$.
    \item Side-by-side avec la baseline Météorage pendant 3-6 mois (\og shadow mode \fg{}).
    \item Mesure A/B des gains réels en heures d'opération sauvées.
    \item Si OK, bascule progressive aéroport par aéroport.
    \item Re-fit annuel automatisé sur les nouvelles données.
\end{enumerate}
\end{piege}

\begin{piege}
\textbf{Q : Et si Météorage change ses détecteurs ?}

R : Le modèle dépend des caractéristiques de détection (sensibilité, fréquence d'IC vs CG).
Un changement de capteurs créerait une dérive — il faudrait re-fit. Pour le détecter,
on monitorerait la distribution des features (data drift) avec des tests KS ou PSI.
\end{piege}

\begin{piege}
\textbf{Q : Le modèle est-il transférable à d'autres aéroports ?}

R : Partiellement. Les coefficients $\beta$ sur les 17 features (rythme, distance, intensité,
saison) sont \emph{universels} — un orage actif dans la Bresse se comporte comme un orage
actif à Ajaccio sur ces variables. Les dummies aéroport capturent les spécificités locales
(climat, géographie). Pour un nouvel aéroport, on aurait besoin de 6-12 mois de données pour
estimer son effet local. Ou bien : utiliser le modèle sans dummy aéroport (= généraliste) et
accepter une perte de C-index de quelques points.
\end{piege}

\begin{piege}
\textbf{Q : Avez-vous testé d'autres distributions (Gamma, log-normale, exponentielle) ?}

R : Brièvement. L'exponentielle est un cas particulier de Weibull avec $k = 1$ — on l'a
rejetée par AIC. Le log-normale donne des résultats similaires en C-index mais ses quantiles
extrêmes sont moins stables. La distribution généralisée de Pareto (queue heavy-tailed)
pourrait améliorer la calibration mais lifelines ne la propose pas en natif. Weibull est
le standard et permet une calibration ex-post efficace.
\end{piege}

% ==========================================================================
\section*{Mémo express — 1 minute avant l'oral}

\begin{tcolorbox}[colback=red!4, colframe=red!50!black, title=\textbf{Les 5 phrases-clés à ne pas oublier}]
\begin{enumerate}
    \item Modèle \textbf{Weibull AFT} entraîné par MLE qui gère la censure à 30 min nativement.
    \item \textbf{17 variables}, le rythme (rolling\_gap\_5) concentre 57 \% du pouvoir prédictif.
    \item \textbf{Calibration robuste} : winsorization + monotonicité ($c_{95} = 2{,}02$).
    \item \textbf{Protocole officiel reproduit} dans \texttt{Evaluation\_databattle\_weibull.ipynb}.
    \item Mesure principale à $q=0{,}95$ : \textbf{0 \% CG / 330 h gagnées} sur 1\,352 alertes.
    Mesure sweep $\theta$ strict à $\theta=0{,}99$ : \textbf{2,86 \% / 26 h}.
    Baseline 30 min : \textbf{0 \%} (référence parfaite par construction).
\end{enumerate}
\end{tcolorbox}

\begin{tcolorbox}[colback=blue!4, colframe=blue!50!black, title=\textbf{Lancer la démo}]
\texttt{streamlit run app.py}

(à lancer dans un terminal positionné dans \texttt{oral\_projet/})
\end{tcolorbox}

\end{document}
